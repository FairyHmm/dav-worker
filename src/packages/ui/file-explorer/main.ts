// Vite entry point — mounts the resource's single Svelte 5 app into
// #app (see index.html). This is the client-side half of the File
// resource per SPEC-UI.md: tree view + breadcrumbs + metadata panel +
// context menu (dir_*/entry_* tools), plus markdown preview/edit +
// outline sidebar (file_* tools) — one shared interface per resource,
// not one UI per tool.
//
// STUB — App.svelte not yet written. Filled in during the next pass
// (tree view first, per build order: File -> Tasks -> Calendar).
//
//   import { mount } from "svelte";
//   import App from "./App.svelte";
//
//   mount(App, { target: document.getElementById("app")! });
