// Vite entry point — mounts the Config UI Svelte 5 app into #app
// (see index.html). One shared interface for config_get / config_set
// per SPEC-UI-CONFIG.md: Locations, Calendars, and Tools sections.
//
// STUB — App.svelte not yet written.
//
//   import { mount } from "svelte";
//   import App from "./App.svelte";
//
//   mount(App, { target: document.getElementById("app")! });
