// Barrel — package surface for workspace. The worker needs the built
// dist/index.html (viteSingleFile output) served as ui://config, not a
// TS import. This file exists so the package is a well-formed workspace
// member.
//
// STUB — nothing to export yet.
export {};
